import * as React from "react";
import MapWrapper from "./map";

function Index() {
    return (
        <main>
            <div>
                <MapWrapper />
            </div>
        </main>
    );
}

export default Index;
